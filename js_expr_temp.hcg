## HYDROCARBON BASE JS ##

#SYMBOLS << >>> >> >>>= >>= <<= <= >= != !== |= === == ** ++ -- **= ... || && => ** ++ -- **= ... || && += -= /= *= %=  &= |=

#IGNORE θws θnl

S → statement	

statement →  τm danger τt ;	
danger → ( ; ; )      	↦ 1 env.ASI = false;
						↦ 4 env.ASI = true;
